-- Adds a second (evening) showtime slot, on a different screen ("hall") than the existing
-- matinee slot, for every movie/city/week -- doubling show density everywhere.
--
-- This file intentionally contains no data-generating SQL. The original version of this
-- migration ran the generation query directly (recursive CTE + two ROW_NUMBER() OVER
-- window functions + a large NOT EXISTS anti-join), and on the hosted MySQL that reliably
-- failed with "Error 1114: table '/tmp/#sql...' is full" -- not because the data itself is
-- large (the final table is ~110MB), but because MySQL's tmpdir for on-disk temp tables
-- (used to materialize the window-function/CTE intermediate results and the NOT EXISTS
-- check) appears to sit on a small filesystem separate from the persistent data volume, and
-- even a plain `CREATE INDEX` on the existing 576k-row table hit the identical error. That
-- rules out the migration's own complexity as the cause -- it's a platform-level ceiling on
-- temp-table disk space that no rewrite of this query avoids.
--
-- The target data was instead generated by running the same generation query directly
-- against the database in smaller batches (which stay under the temp-table ceiling) via a
-- one-off connection, and was verified afterwards: 576,189 total showtime rows, all 298
-- movies covered, evening slots landing on a different screen than their matching matinee
-- slot. This file exists only so Flyway's schema history reflects that V18 has been
-- applied, matching the data that's actually there.
SELECT 1;
